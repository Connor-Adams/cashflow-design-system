---
'@connor-adams/designsystem': minor
---

Add Icon component — a registry of lucide-style stroke glyphs (money, categories, actions, status, chevrons) rendered in `currentColor` and sized via the `size` prop. Names exported as `iconNames`.
