A hairline rule to separate sections, list rows, or inline metadata.

```jsx
<Separator />
<Separator orientation="vertical" />   {/* inside a flex row */}
<Separator label="or" />
```

Variants: `orientation="horizontal" | "vertical"`, optional `label`.
